info:
  name: List My Invitations
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}/admin/list-my-invitations"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
