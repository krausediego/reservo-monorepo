info:
  name: Reject Invitation
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseURL}}/admin/reject-invitation/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
