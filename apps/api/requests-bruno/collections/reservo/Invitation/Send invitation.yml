info:
  name: Send invitation
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseURL}}/invitation"
  body:
    type: json
    data: |-
      {
        "email": "diegoemanuelk@gmail.comm",
        "role": "MANAGER"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
