info:
  name: List professionals
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}/professionals"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
