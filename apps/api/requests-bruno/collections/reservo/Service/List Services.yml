info:
  name: List Services
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}/services"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
